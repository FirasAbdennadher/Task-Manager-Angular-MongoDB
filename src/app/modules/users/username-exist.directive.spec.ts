import { UsernameExistDirective } from './username-exist.directive';

describe('UsernameExistDirective', () => {
  it('should create an instance', () => {
    const directive = new UsernameExistDirective();
    expect(directive).toBeTruthy();
  });
});
