import { CoreDirective } from './core.directive';

describe('CoreDirective', () => {
  it('should create an instance', () => {
    const directive = new CoreDirective();
    expect(directive).toBeTruthy();
  });
});
